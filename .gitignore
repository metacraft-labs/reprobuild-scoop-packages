.repro/
build/

# `nim c -r` drops the linked binary beside its source unless --out
# points elsewhere, and the test task in repro.nim does not.
*.exe
*.pdb

# Generated by the workspace flake hook.
.pre-commit-config.yaml
# Per-checkout nimcache (config.nims): every Nim compile in this checkout
# keeps its intermediate files here, never in the shared ~/.cache/nim.
.nimcache/
